{
  "name": "claudefob",
  "version": "0.1.0",
  "description": "Unofficial CLI to store multiple Claude Code OAuth tokens in your OS keystore and switch between them. Not affiliated with Anthropic.",
  "type": "module",
  "bin": {
    "claudefob": "./dist/cli.js"
  },
  "files": ["dist", "README.md", "LICENSE"],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "keywords": ["claude", "claude-code", "oauth", "token", "keychain", "keyring", "credential-manager", "cli"],
  "scripts": {
    "build": "bun build src/cli.ts --target node --outdir dist --external @napi-rs/keyring --banner '#!/usr/bin/env node' && chmod +x dist/cli.js",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "@napi-rs/keyring": "^1.3.0",
    "citty": "^0.2.2",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  }
}
